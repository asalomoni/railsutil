<%= form_for(@ball) do |f| %>
  <% if @ball.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ball.errors.count, "error") %> prohibited this ball from being saved:</h2>

      <ul>
      <% @ball.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ball_type %><br>
    <%= f.text_field :ball_type %>
  </div>
  <div class="field">
    <%= f.label :ball_size %><br>
    <%= f.text_field :ball_size %>
  </div>
  <div class="field">
    <%= f.label :ball_weight %><br>
    <%= f.text_field :ball_weight %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
